(function(exports) {
  'use strict';
  /* jshint browser:true */

  var join = {};
  exports.join = join;

  join.init = function() {

    var ractive = templates.moveToPage('join');
    templates.setTopNav('Join a Class', true);

  };

}(window));
