<p class="content-padded">
  Welcome to your room, please give feedback on how the class is going.
</p>

<div class="content-padded roomID">
  Room ID: <span class="roomID--value">{{roomId}}</span>
</div>

<form>
  <div class="input-row">
    <label>Happiness</label>
    <input type="range" min="0" max="100" value="{{happiness}}" />
  </div>
</form>

<button class="btn btn-block" on-tap='sendConfused'>Confused</button>

<form>
  <div class="input-row">
    <label>Comment</label>
    <input type="text" value='{{message}}'>
  </div>
  <button class="btn btn-block" on-tap='sendMessage'>Send</button>
</form>
